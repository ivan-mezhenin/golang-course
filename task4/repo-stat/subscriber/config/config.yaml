app:
  app_name: "repo-stat-subscriber"

grpc:
  address: "localhost:8081"
  timeout: "5s"

logger:
  log_level: "DEBUG"

services:
  api: "localhost:8080"

database:
  host: "postgres"
  port: 5432
  user: "admin"
  password: "password"
  dbname: "repo_stat"
  sslmode: "disable"